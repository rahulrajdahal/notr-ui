{
  "name": "notr-ui",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "publish": "changeset publish"
  },
  "devDependencies": {
    "turbo": "latest",
    "@changesets/cli": "latest"
  }
}
